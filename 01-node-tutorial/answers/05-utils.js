function message(name, location, school) {
    console.log(`My name is ${name}. I currently live in ${location}. I graduated from the ${school}.`);
}

// export function module
module.exports = message;